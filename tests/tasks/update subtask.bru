meta {
  name: update subtask
  type: http
  seq: 6
}

put {
  url: http://localhost:3000/api/tasks/{{id}}/subtasks/{{subtaskid}}
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "titre": "subtask test updated"
  }
}

tests {
  test("Should update created subtask", () => {
    const body = res.getBody();
  
    expect(res.getStatus()).to.equal(200);
    expect(body.success).to.equal(true);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
